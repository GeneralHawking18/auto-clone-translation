# Mô tả:
Người dùng sẽ bấm chọn các element có text, và bấm phím tắt sau đó system sẽ tự lấy ra các element tương ứng  của từng chỗ và dịch sang ngôn ngữ cần dịch, sẽ có bảng chọn, và hiển ra danh sách các element, (đó là nhiệm vụ của UI) 

Khi user chọn xong, bấm proceed thì sẽ tự động dịch sang ngôn ngũ khác và font cần chọn khi đó, hệ thống sẽ tự dùng AI để dịch và clone ra file mới.

Hãy tìm cách trong adobe có thể đọc được text bên trong cái element của cái đang chọn tìm cách


Tôi vẫn cần một giao diện UI trên adobe illustatro khi họ chọn các element có text, nó sẽ hiện ra các text sẽ chuẩn bị được process và bấm phím tắt, sau đó hệ thống sẽ tự động hiện UI ra, và họ sẽ chọn ngôn ngữ và font trong hệ thóng và sau đó dịch và clone ra các elemtn mới như trong logic sẵn có